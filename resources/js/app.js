import './bootstrap';
import './sidebar';
import { createIcons, LayoutGrid, Zap, Network, Settings, Briefcase, Building, ChevronDown } from 'lucide';

// Saat inisialisasi, gunakan nama yang diimport:
createIcons({
    icons: {
        LayoutGrid,
        Zap,
        Network,
        Settings,
        Briefcase,
        Building,
        ChevronDown
    }
});
